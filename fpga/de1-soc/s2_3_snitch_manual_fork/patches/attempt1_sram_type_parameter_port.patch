--- original/.bender/git/checkouts/tech_cells_generic-1282165f7b690985/src/rtl/tc_sram.sv
+++ s2_3/.bender/git/checkouts/tech_cells_generic-1282165f7b690985/src/rtl/tc_sram.sv
@@ -65,37 +65,34 @@
   parameter              FPGAImplKey  = "auto",   // Reference to specific implementation for fpga
   // DEPENDENT PARAMETERS, DO NOT OVERWRITE!
   parameter int unsigned AddrWidth = (NumWords > 32'd1) ? $clog2(NumWords) : 32'd1,
-  parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth, // ceil_div
-  parameter type         addr_t    = logic [AddrWidth-1:0],
-  parameter type         data_t    = logic [DataWidth-1:0],
-  parameter type         be_t      = logic [BeWidth-1:0]
+  parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth  // ceil_div
 ) (
   input  logic                 clk_i,      // Clock
   input  logic                 rst_ni,     // Asynchronous reset active low
   // input ports
   input  logic  [NumPorts-1:0] req_i,      // request
   input  logic  [NumPorts-1:0] we_i,       // write enable
-  input  addr_t [NumPorts-1:0] addr_i,     // request address
-  input  data_t [NumPorts-1:0] wdata_i,    // write data
-  input  be_t   [NumPorts-1:0] be_i,       // write byte enable
+  input  logic [NumPorts-1:0][AddrWidth-1:0] addr_i,     // request address
+  input  logic [NumPorts-1:0][DataWidth-1:0] wdata_i,    // write data
+  input  logic [NumPorts-1:0][BeWidth-1:0] be_i,       // write byte enable
   // output ports
-  output data_t [NumPorts-1:0] rdata_o     // read data
+  output logic [NumPorts-1:0][DataWidth-1:0] rdata_o     // read data
 );
 
   // memory array
-  data_t sram [NumWords-1:0];
+  logic [DataWidth-1:0] sram [NumWords-1:0];
   // hold the read address when no read access is made
-  addr_t [NumPorts-1:0] r_addr_q;
+  logic [NumPorts-1:0][AddrWidth-1:0] r_addr_q;
 
   // SRAM simulation initialization
-  function automatic data_t random_init_word();
+  function automatic logic [DataWidth-1:0] random_init_word();
     random_init_word = '0;
     for (int unsigned b = 0; b < DataWidth; b += 32) begin
-      random_init_word = (random_init_word << 32) | data_t'($urandom());
+      random_init_word = (random_init_word << 32) | $urandom();
     end
   endfunction
 
-  data_t init_val[NumWords-1:0];
+  logic [DataWidth-1:0] init_val [NumWords-1:0];
   initial begin : proc_sram_init
     for (int unsigned i = 0; i < NumWords; i++) begin
       unique case (SimInit)
@@ -113,7 +110,7 @@
   // array index 0.
 
   // read data output assignment
-  data_t [NumPorts-1:0][Latency-1:0] rdata_q,  rdata_d;
+  logic [NumPorts-1:0][Latency-1:0][DataWidth-1:0] rdata_q, rdata_d;
   if (Latency == 32'd0) begin : gen_no_read_lat
     for (genvar i = 0; i < NumPorts; i++) begin : gen_port
       assign rdata_o[i] = (req_i[i] && !we_i[i]) ? sram[addr_i[i]] : sram[r_addr_q[i]];
--- original/.bender/git/checkouts/tech_cells_generic-1282165f7b690985/src/rtl/tc_sram_impl.sv
+++ s2_3/.bender/git/checkouts/tech_cells_generic-1282165f7b690985/src/rtl/tc_sram_impl.sv
@@ -33,29 +33,24 @@
   parameter              SimInit      = "none",   // Simulation initialization
   parameter bit          PrintSimCfg  = 1'b0,     // Print configuration
   parameter              ImplKey      = "none",   // Reference to specific implementation
-  parameter type         impl_in_t    = logic,    // Type for implementation inputs
-  parameter type         impl_out_t   = logic,    // Type for implementation outputs
-  parameter impl_out_t   ImplOutSim   = 'X,       // Implementation output in simulation
+  parameter              ImplOutSim   = 1'b0,     // Implementation output in synthesis patch
   // DEPENDENT PARAMETERS, DO NOT OVERWRITE!
   parameter int unsigned AddrWidth = (NumWords > 32'd1) ? $clog2(NumWords) : 32'd1,
-  parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth, // ceil_div
-  parameter type         addr_t    = logic [AddrWidth-1:0],
-  parameter type         data_t    = logic [DataWidth-1:0],
-  parameter type         be_t      = logic [BeWidth-1:0]
+  parameter int unsigned BeWidth   = (DataWidth + ByteWidth - 32'd1) / ByteWidth  // ceil_div
 ) (
   input  logic                 clk_i,      // Clock
   input  logic                 rst_ni,     // Asynchronous reset active low
   // implementation-related IO
-  input  impl_in_t             impl_i,
-  output impl_out_t            impl_o,
+  input  logic                 impl_i,
+  output logic                 impl_o,
   // input ports
   input  logic  [NumPorts-1:0] req_i,      // request
   input  logic  [NumPorts-1:0] we_i,       // write enable
-  input  addr_t [NumPorts-1:0] addr_i,     // request address
-  input  data_t [NumPorts-1:0] wdata_i,    // write data
-  input  be_t   [NumPorts-1:0] be_i,       // write byte enable
+  input  logic [NumPorts-1:0][AddrWidth-1:0] addr_i,     // request address
+  input  logic [NumPorts-1:0][DataWidth-1:0] wdata_i,    // write data
+  input  logic [NumPorts-1:0][BeWidth-1:0] be_i,       // write byte enable
   // output ports
-  output data_t [NumPorts-1:0] rdata_o     // read data
+  output logic [NumPorts-1:0][DataWidth-1:0] rdata_o     // read data
 );
 
   // We drive a static value for `impl_o` in behavioral simulation.
--- original/hw/snitch_cluster/src/snitch_cluster.sv
+++ s2_3/hw/snitch_cluster/src/snitch_cluster.sv
@@ -702,12 +702,11 @@
         .DataWidth (NarrowDataWidth),
         .ByteWidth (8),
         .NumPorts (1),
-        .Latency (1),
-        .impl_in_t (sram_cfg_t)
+        .Latency (1)
       ) i_data_mem (
         .clk_i,
         .rst_ni,
-        .impl_i (sram_cfgs_i.tcdm),
+        .impl_i (1'b0),
         .impl_o (  ),
         .req_i (mem_cs),
         .we_i (mem_wen),
--- original/hw/snitch_icache/src/snitch_icache_lookup.sv
+++ s2_3/hw/snitch_icache/src/snitch_icache_lookup.sv
@@ -150,12 +150,11 @@
           .DataWidth (CFG.TAG_WIDTH+2),
           .ByteWidth (8),
           .NumPorts (1),
-          .Latency (1),
-          .impl_in_t (sram_cfg_tag_t)
+          .Latency (1)
         ) i_tag (
           .clk_i (clk_i),
           .rst_ni (rst_ni),
-          .impl_i (sram_cfg_tag_i),
+          .impl_i (1'b0),
           .impl_o (  ),
           .req_i (ram_enable[i]),
           .we_i (ram_write),
@@ -170,12 +169,11 @@
           .DataWidth (CFG.LINE_WIDTH),
           .ByteWidth (8),
           .NumPorts (1),
-          .Latency (1),
-          .impl_in_t (sram_cfg_data_t)
+          .Latency (1)
         ) i_data (
           .clk_i (clk_i),
           .rst_ni (rst_ni),
-          .impl_i (sram_cfg_data_i),
+          .impl_i (1'b0),
           .impl_o (  ),
           .req_i (ram_enable[i]),
           .we_i (ram_write),
